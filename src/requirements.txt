# GroceryApp dependencies
# Install with: pip install -r requirements.txt

appJar>=0.94          # GUI framework for the Menu Builder window
requests>=2.28        # Trello REST API calls
todoist-api-python>=2.1  # Todoist SDK

# Standard-library modules used (no install needed):
#   pathlib, sqlite3, glob, datetime, random, unittest
